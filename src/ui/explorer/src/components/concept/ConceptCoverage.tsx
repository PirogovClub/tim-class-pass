import { Link } from 'react-router-dom';
export function ConceptCoverage({ lessons }: { lessons: string[] }) { return <section className="space-y-2"><h2 className="text-lg font-semibold text-slate-900">Lesson Coverage</h2>{lessons.length ? <div className="flex flex-wrap gap-2">{lessons.map((lesson) => <Link key={lesson} to={`/lesson/${encodeURIComponent(lesson)}`} className="rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-xs font-medium text-slate-700 hover:border-teal-300 hover:text-teal-700">{lesson}</Link>)}</div> : <p className="text-sm text-slate-500">No linked lessons</p>}</section>; }
