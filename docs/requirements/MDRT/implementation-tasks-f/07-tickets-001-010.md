# MDRT Implementation Plan — Section 8+12: First 10 Tickets (Fully Specified)

---

## MDRT-001: Project Scaffold and pyproject.toml

### Purpose
Create the project skeleton with all directories, `pyproject.toml`, `src/market_data/.env.example`, `.gitignore`, and empty `__init__.py` files.

### Source Docs
- `01-architecture.md` §Directory Structure, §Phase 1 Dependencies

### Exact Requirements from Docs
- Directory tree exactly matches `01-architecture.md` lines 215–278
- `pyproject.toml` with deps: `ibapi>=9.81` (PyPI-installable baseline; newer non-PyPI/manual installation remains possible if adopted later), `pyarrow>=14.0`, `duckdb>=0.10`, `typer>=0.9`, `pydantic-settings>=2.0`, `python-dotenv>=1.0`
- Dev deps: `pytest>=7.0`, `pytest-cov>=4.0`, `ruff>=0.3`
- Entry point: `mdrt = "market_data.cli.main:app"`
- `requires-python = ">= 3.11"`

### In Scope
- Create all directories listed in `01-architecture.md`
- Create `pyproject.toml` with exact deps
- Create **minimal placeholder** `src/market_data/.env.example` with IB connection vars from `08-configuration.md` (final content owned by MDRT-020)
- Create `.gitignore` for `data/`, `outputs/`, `*.duckdb`, `__pycache__/`
- Create empty `__init__.py` in all package directories
- Create minimal `src/market_data/cli/main.py` with empty Typer app
- Create **minimal placeholder** `src/market_data/README.md` with project title and one-line description (final content owned by MDRT-020)

### Out of Scope
- Actual implementation of any module
- Test files (created per module in later tickets)
- Final `src/market_data/.env.example` content or operational documentation (MDRT-020)

### Expected Files
```
pyproject.toml
.gitignore
src/market_data/__init__.py
src/market_data/.env.example
src/market_data/README.md
src/market_data/cli/__init__.py
src/market_data/cli/main.py
src/market_data/adapters/__init__.py
src/market_data/core/__init__.py
src/market_data/models/__init__.py
src/market_data/config/__init__.py
tests/market_data/__init__.py (optional)
tests/market_data/conftest.py (empty placeholder)
```

### Required Tests
- `uv sync` succeeds
- `python -c "import market_data"` succeeds
- `uv run mdrt --help` outputs Typer help (even if no commands yet)

### Definition of Done
- [ ] All directories exist as specified in `01-architecture.md`
- [ ] `uv sync` succeeds on Python ≥ 3.11
- [ ] `uv run mdrt --help` runs without error
- [ ] `src/market_data/.env.example` contains IB vars from `08-configuration.md`
- [ ] `src/market_data/README.md` exists as minimal placeholder
- [ ] `tests/market_data/conftest.py` exists as empty placeholder

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-001/`
- Files: `summary.md`, `changed-files.txt`, `test-results.txt`

### Dependencies
- None (first ticket)

### Risk Level
- **Low**

---

## MDRT-002: Exception Hierarchy

### Purpose
Implement the complete exception class hierarchy so all downstream modules can import and raise the correct exceptions.

### Source Docs
- `07-exceptions.md` (entire document)

### Exact Requirements from Docs
- Full exception tree from `07-exceptions.md` §7.1
- Complete source code from `07-exceptions.md` §7.2
- IB error code mapping table from §7.3 (as constants or dict)
- CLI exit code mapping from §7.5 (as helper function or dict)

### In Scope
- `src/market_data/exceptions.py` — full hierarchy
- All classes: `ProviderSessionError`, `ProviderConnectError`, `ProviderReadyError`, `ProviderAuthError`, `ProviderRateLimitError`, `ProviderPacingError`, `ProviderDataError`, `EmptyResponseError`, `ContractResolutionError`, `AmbiguousContractError`, `ContractNotFoundError`, `UnsupportedTimeframeError`, `NormalizationError`, `MissingRequiredFieldError`, `SchemaConformanceError`, `ValidationError`, `DuplicateTimestampError`, `NonMonotonicTimeError`, `InvalidOHLCError`, `NegativePriceError`, `NegativeVolumeError`, `ArchiveWriteError`, `PartitionPathError`, `CatalogError`, `WindowBuildError`, `WindowAnchorNotFoundError`, `InsufficientBarsError`, `DataQualityWarning`, `DataGapWarning`, `LowVolumeWarning`, `TimezoneInconsistencyWarning`
- IB error code to exception mapping dict
- CLI exit code mapping dict

### Out of Scope
- Usage of these exceptions in other modules

### Expected Files
```
src/market_data/exceptions.py
tests/unit/test_exceptions.py
```

### Required Tests
- Every exception class is importable
- Inheritance chain is correct (`ProviderConnectError` → `ProviderSessionError` → `Exception`)
- IB error code mapping returns correct exception type for codes: 162, 200, 1100, 1102
- CLI exit code mapping returns correct codes for each exception class

### Definition of Done
- [ ] All 30+ exception classes defined
- [ ] All inheritance relationships match `07-exceptions.md` tree
- [ ] IB error code map covers all codes in §7.3
- [ ] CLI exit code map covers all entries in §7.5
- [ ] `tests/unit/test_exceptions.py` passes

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-002/`

### Dependencies
- MDRT-001 (project scaffold)

### Risk Level
- **Low**

---

## MDRT-003: Domain Models (Dataclasses)

### Purpose
Implement all domain dataclasses so other modules can import `Bar`, `Instrument`, `RequestSpec`, `ProviderSessionInfo`, `ContractResolutionRecord`, `ArchiveFileRecord`, `ValidationReport`, `WindowRequest`.

### Source Docs
- `02-data-models.md` §2.1–§2.8 (all dataclasses)

### Exact Requirements from Docs
- `Bar` dataclass with all fields from `02-data-models.md`:
  - `ts_utc`, `session_date`, `session_close_ts_utc`, OHLCV, `trade_count`, `vwap`, `session_code`, `source_tz`, `ingested_at`, `source_batch_id`, `request_spec_id`
  - Partition keys: `provider`, `symbol`, `timeframe`, `asset_class`, `use_rth`, `what_to_show`
  - `source_tz` comment: "TWS login/session timezone captured from ProviderSessionInfo"
  - `session_close_ts_utc`: null for intraday, non-null for `1D` and `1M`
- `Instrument` dataclass with `instrument_id`, `provider`, `symbol`, `con_id`, `exchange`, etc.
- `RequestSpec` with `timeframe` field supporting: `"1m" | "5m" | "15m" | "1h" | "4h" | "1D" | "1M"`
- `ProviderSessionInfo` with `tws_login_timezone`
- `ContractResolutionRecord`, `ArchiveFileRecord`, `ValidationReport`, `WindowRequest`
- All dataclass invariant comments from §2.7

### In Scope
- `src/market_data/models/domain.py` — all dataclasses
- Unit tests for field defaults, types, and invariant documentation

### Out of Scope
- PyArrow schema (MDRT-005)
- DuckDB DDL (MDRT-005)

### Expected Files
```
src/market_data/models/domain.py
tests/unit/test_domain.py
```

### Required Tests
- Every dataclass instantiable with valid arguments
- `Bar` has `session_date` field (not None)
- `RequestSpec.timeframe` accepts all 7 supported values
- `ProviderSessionInfo.tws_login_timezone` field exists
- `ArchiveFileRecord.superseded_by` field exists (Optional)
- `request_hash` generation for `RequestSpec` is deterministic

### Definition of Done
- [ ] All 8+ dataclasses defined in `domain.py`
- [ ] All field types match `02-data-models.md`
- [ ] `request_hash` generation implemented
- [ ] `tests/unit/test_domain.py` passes

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-003/`

### Dependencies
- MDRT-001 (project scaffold)

### Risk Level
- **Low**

---

## MDRT-004: Configuration (Pydantic Settings)

### Purpose
Implement the `MdrtSettings` Pydantic Settings class with all env vars, defaults, and `.env` loading.

### Source Docs
- `08-configuration.md` (entire document)
- `01-architecture.md` §Settings location

### Exact Requirements from Docs
- Canonical location: `src/market_data/config/settings.py`
- IB vars: `IB_HOST`, `IB_PORT`, `IB_CLIENT_ID`, `IB_TWS_LOGIN_TIMEZONE`, `IB_PACING_DELAY_SEC`, `IB_CONNECT_TIMEOUT_SEC`, `IB_MAX_RETRIES`
- Data vars: `MDRT_DATA_DIR`, `MDRT_OUTPUT_DIR`
- Required defaults from `08-configuration.md`: `IB_PACING_DELAY_SEC=15`, `IB_TWS_LOGIN_TIMEZONE="America/New_York"`
- `IB_TWS_LOGIN_TIMEZONE` is NOT the machine/OS timezone — it is the TWS session timezone
- Port table from `08-configuration.md`: TWS Live=7496, TWS Paper=7497, Gateway Live=4001, Gateway Paper=4002
- `mock_settings` fixture pattern from `09-testing.md` conftest

### In Scope
- `src/market_data/config/settings.py` — `MdrtSettings` class
- `tests/market_data/conftest.py` — `mock_settings` autouse fixture
- `tests/unit/test_settings.py` — settings unit tests

### Out of Scope
- Operational documentation
- REST provider config (Phase 2)

### Expected Files
```
src/market_data/config/settings.py
tests/market_data/conftest.py
tests/unit/test_settings.py
```

### Required Tests
- Settings loads from env vars
- Settings loads from `.env` file
- Default values match `08-configuration.md`
- `IB_TWS_LOGIN_TIMEZONE` default = "America/New_York"
- `mock_settings` fixture prevents reading live `.env`
- Settings rejects invalid `IB_PORT` (must be a positive integer in valid TCP port range 1–65535; known IB ports 7496/7497/4001/4002 are documented defaults, NOT an exclusive allowlist)

### Definition of Done
- [ ] `MdrtSettings` importable and instantiable
- [ ] All env vars from `08-configuration.md` present
- [ ] Defaults match spec exactly
- [ ] `mock_settings` autouse fixture in `tests/market_data/conftest.py`
- [ ] `tests/unit/test_settings.py` passes

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-004/`

### Dependencies
- MDRT-001 (project scaffold)

### Risk Level
- **Low**

---

## MDRT-005: PyArrow Schema v3 + DuckDB DDL

### Purpose
Implement the single source of truth for the normalized bar schema (PyArrow) and all 8 DuckDB catalog tables.

### Source Docs
- `02-data-models.md` §PyArrow Schema v3, §DuckDB DDL
- `04-core-pipeline.md` (normalizer output type reference)

### Exact Requirements from Docs
- `NORMALIZED_BAR_SCHEMA` v3 with exact fields from `02-data-models.md`:
  - `provider`, `asset_class`, `symbol`, `timeframe`, `use_rth`, `what_to_show`
  - `ts_utc` (timestamp[us, UTC], NOT NULL)
  - `session_date` (date32, NOT NULL)
  - `session_close_ts_utc` (timestamp[us, UTC], nullable — non-null for 1D and 1M)
  - OHLCV, `trade_count`, `vwap`, `session_code`, `source_tz`, `ingested_at`, `source_batch_id`, `request_spec_id`
- DuckDB DDL for all 8 tables: `instruments`, `provider_sessions`, `request_specs`, `ingestion_batches`, `archive_file_records`, `archive_coverage`, `window_log`, `data_quality_events`
- All DDL uses `IF NOT EXISTS` (cross-phase constraint from `10-phases.md`)
- Schema version constant: `SCHEMA_VERSION = 3`

### In Scope
- `src/market_data/models/schemas.py` — PyArrow schema
- `src/market_data/models/catalog_sql.py` — DuckDB DDL strings
- Unit tests for schema conformance

### Out of Scope
- Actual DuckDB connection management (MDRT-014)
- Writing Parquet files (MDRT-015)

### Expected Files
```
src/market_data/models/schemas.py
src/market_data/models/catalog_sql.py
tests/unit/test_schemas.py
```

### Required Tests
- `NORMALIZED_BAR_SCHEMA` has exactly the fields listed in `02-data-models.md`
- `session_date` is NOT NULL
- `session_close_ts_utc` is nullable
- `ts_utc` is `timestamp[us, tz=UTC]`
- All 8 DDL strings are valid SQL
- `SCHEMA_VERSION` constant = 3
- Sample `pa.Table` created from schema validates correctly

### Definition of Done
- [ ] `NORMALIZED_BAR_SCHEMA` matches spec exactly (field count, types, nullability)
- [ ] All 8 DDL strings defined
- [ ] DDL uses `IF NOT EXISTS` for all tables
- [ ] `tests/unit/test_schemas.py` passes

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-005/`

### Dependencies
- MDRT-001 (scaffold), MDRT-003 (domain models for type reference)

### Risk Level
- **Low** (but this is an **audit gate** — must be reviewed before proceeding)

---

## MDRT-006: Trading Calendar (NYSE/Nasdaq Hardcoded)

### Purpose
Implement the hardcoded NYSE/Nasdaq trading calendar with exact early-close handling for Phase 1.

### Source Docs
- `10-phases.md` §Calendar Exact Specification
- `04-core-pipeline.md` §TRAP 4 (early-close handling)
- `02-data-models.md` §Calendar Model, §CALENDAR_APPROXIMATION
- `09-testing.md` §Calendar Correctness Regression Tests

### Exact Requirements from Docs
- Hardcoded NYSE holidays: New Year's, MLK, Presidents', Good Friday, Memorial, Juneteenth, July 4th, Labor Day, Thanksgiving, Christmas
- NYSE is OPEN on Veterans Day and Columbus Day (federal holidays)
- Known early closes (13:00 ET): day before July 4th, day after Thanksgiving, Christmas Eve (weekday)
- Early closes return exact 13:00 ET close time, NOT 16:00 ET
- `CALENDAR_APPROXIMATION` only for unscheduled closures (not early closes)
- Interface: `is_trading_day(date)`, `get_session_close(date)` returning exact close time

### In Scope
- `src/market_data/core/trading_calendar.py` — `TradingCalendar` class
- Calendar data for years 2020–2030 (reasonable Phase 1 range)
- Calendar regression tests from `09-testing.md`

### Out of Scope
- `exchange_calendars` library integration (Phase 3)
- Non-US exchanges

### Expected Files
```
src/market_data/core/trading_calendar.py
tests/unit/test_trading_calendar.py
```

### Required Tests (from `09-testing.md`)
- `test_good_friday_classified_as_market_closed` — Good Friday → market closed
- `test_veterans_day_not_classified_as_closed` — Veterans Day → market OPEN
- `test_columbus_day_not_classified_as_closed` — Columbus Day → market OPEN
- `test_day_after_thanksgiving_early_close_exact` — returns 13:00 ET exactly
- `test_christmas_eve_early_close_exact` — returns 13:00 ET exactly
- `test_early_close_session_returns_1300_et` — `get_session_close()` returns 13:00 ET (18:00 UTC) for known early-close days
- `test_unscheduled_closure_produces_approximation` — unknown closure → `CALENDAR_APPROXIMATION`
- `test_normal_trading_day` — returns 16:00 ET (21:00 UTC)
- `test_weekend_not_trading_day` — Saturday/Sunday → not trading

### Definition of Done
- [ ] `TradingCalendar` has `is_trading_day()` and `get_session_close()` methods
- [ ] Holidays list matches `10-phases.md` exactly
- [ ] Early closes return 13:00 ET, NOT 16:00 ET
- [ ] All 9+ calendar regression tests pass
- [ ] Calendar data covers years 2020–2030

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-006/`

### Dependencies
- MDRT-001 (scaffold)

### Risk Level
- **Medium** (calendar correctness is critical for downstream gap classification)

---

## MDRT-007: ProviderSession ABC + IbSession

### Purpose
Implement the `ProviderSession` abstract base class and the IB-specific `IbSession` that manages the TCP socket lifecycle to TWS/Gateway.

### Source Docs
- `03-adapter-interface.md` §3.1 (ProviderSession ABC, IbSession)
- `07-exceptions.md` (ProviderSessionError, ProviderConnectError, ProviderReadyError)
- `08-configuration.md` (IB connection settings)
- `09-testing.md` (CallbackReplaySession concept)

### Exact Requirements from Docs
- ABC with `connect()`, `disconnect()`, `ensure_ready()`, `session_info` property
- `IbSession.ensure_ready()`: waits for `nextValidId` callback; raises `ProviderReadyError` on timeout
- Returns `ProviderSessionInfo` with `tws_login_timezone` from settings (config-only, NOT from API)
- Handles errors 1100/1102 → `ProviderSessionError`, fail batch immediately
- Port config from settings: 7496/7497/4001/4002
- `CallbackReplaySession` stub for offline tests
- `IB_CONNECT_TIMEOUT_SEC` from settings

### In Scope
- `src/market_data/adapters/session.py` — ABC + `IbSession`
- `src/market_data/adapters/base.py` — re-exports
- Stub `CallbackReplaySession` for tests
- Unit tests with replay session (no real IB connection)

### Out of Scope
- `RestSession` (Phase 2)
- ContractResolver, Collector (separate tickets)

### Expected Files
```
src/market_data/adapters/session.py
src/market_data/adapters/base.py
tests/adapters/test_ib_session.py
```

### Required Tests
- `IbSession` instantiable with mock connection
- `ensure_ready()` succeeds after `nextValidId` callback via replay
- `ensure_ready()` raises `ProviderReadyError` on timeout
- `session_info` returns `ProviderSessionInfo` with correct timezone
- Error 1100 raises `ProviderSessionError`
- `disconnect()` is clean (no exception on double-disconnect)

### Definition of Done
- [ ] `ProviderSession` ABC defined with correct methods
- [ ] `IbSession` implements ABC
- [ ] `CallbackReplaySession` works for offline tests
- [ ] `session_info.tws_login_timezone` comes from settings
- [ ] Error handling matches `07-exceptions.md`
- [ ] `tests/adapters/test_ib_session.py` passes

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-007/`

### Dependencies
- MDRT-001, MDRT-002 (exceptions), MDRT-003 (ProviderSessionInfo), MDRT-004 (settings)

### Risk Level
- **Medium** (ibapi socket lifecycle is the riskiest part)

---

## MDRT-008: ContractResolver ABC + IbContractResolver

### Purpose
Implement the `ContractResolver` ABC and `IbContractResolver` that resolves symbol strings to stable IB contract identities.

### Source Docs
- `03-adapter-interface.md` §3.1a (ContractResolver ABC, IbContractResolver)
- `07-exceptions.md` (ContractResolutionError, AmbiguousContractError, ContractNotFoundError)
- `02-data-models.md` (Instrument, ContractResolutionRecord)

### Exact Requirements from Docs
- ABC with `resolve(symbol, asset_class, exchange)` → `Instrument`
- `IbContractResolver`: calls `reqContractDetails()`, waits for reply
- Stores resolved `conId`, `localSymbol`, `primaryExchange`, `multiplier`, `tradingClass`
- Results cached in-memory inside `IbContractResolver` (simple `dict[str, Instrument]`); DuckDB persistence deferred to MDRT-014 (CatalogManager). Do NOT create a separate `InstrumentRegistry` module
- Second call for same symbol returns cached result from in-memory dict (no API call)
- Raises `AmbiguousContractError` on multiple matches, `ContractNotFoundError` on zero

### In Scope
- `src/market_data/adapters/contract_resolver.py` — ABC + `IbContractResolver`
- In-memory caching inside `IbContractResolver` (dict). No separate InstrumentRegistry class or module. DuckDB persistence added by MDRT-014 (CatalogManager)
- Unit tests via replay session

### Out of Scope
- DuckDB persistence (MDRT-014 — CatalogManager)
- Futures symbology (Phase 3)

### Expected Files
```
src/market_data/adapters/contract_resolver.py
tests/unit/test_contract_resolver.py
```

### Required Tests
- Resolves "SPY" → `Instrument` with `con_id` populated
- Second resolve for "SPY" → returns cached, no API call
- Invalid symbol → `ContractNotFoundError`
- Ambiguous input → `AmbiguousContractError`
- Resolved `Instrument` has all required fields

### Definition of Done
- [ ] `ContractResolver` ABC defined
- [ ] `IbContractResolver` implements ABC
- [ ] Caching works (second call returns cached)
- [ ] Error handling matches `07-exceptions.md`
- [ ] `tests/unit/test_contract_resolver.py` passes

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-008/`

### Dependencies
- MDRT-007 (IbSession for connection), MDRT-003 (Instrument model)

### Risk Level
- **Low-Medium**

---

## MDRT-009: ChunkPlanner + PacingCoordinator

### Purpose
Implement the request chunking logic for IB date ranges and inter-chunk pacing enforcement.

### Source Docs
- `03-adapter-interface.md` §3.3 (ChunkPlanner), §3.3a (PacingCoordinator)
- `09-testing.md` §test_chunk_planner.py

### Exact Requirements from Docs
- ChunkPlanner decomposition rules (from `03-adapter-interface.md`):
  - `1m`: max 30 days per chunk, `durationStr = "2592000 S"` or `"30 D"`
  - `5m`: max 30 days per chunk
  - `15m`: max 30 days per chunk
  - `1h`: max 365 days per chunk, `durationStr = "1 Y"`
  - `4h`: max 365 days per chunk, `durationStr = "1 Y"`
  - `1D`: max 365 days per chunk, `durationStr = "1 Y"`
  - `1M`: max 365 days per chunk, `durationStr = "1 Y"`
- Chunks are **reverse-chronological** (IB `endDateTime` convention)
- All `chunk_end` datetimes are UTC-aware
- PacingCoordinator:
  - `IB_PACING_DELAY_SEC` (default 15s) between chunks
  - Track request rate: ≥ 30 requests in 10 minutes → `is_rate_safe() = False`
  - Raise `ProviderPacingError` when limit reached

### In Scope
- `src/market_data/adapters/chunk_planner.py`
- `src/market_data/adapters/pacing.py`
- Unit tests per `09-testing.md`

### Out of Scope
- Actual IB `reqHistoricalData` calls (MDRT-010)

### Expected Files
```
src/market_data/adapters/chunk_planner.py
src/market_data/adapters/pacing.py
tests/unit/test_chunk_planner.py
tests/unit/test_pacing.py
```

### Required Tests (from `09-testing.md`)
- `test_1m_90_days_produces_3_chunks` — 90-day 1m range → 3×30-day chunks
- `test_1D_365_days_produces_1_chunk` — 365-day 1D range → 1 chunk
- `test_1D_400_days_produces_2_chunks` — 400-day 1D range → 2 chunks
- `test_chunks_are_reverse_chronological` — last chunk first
- `test_chunk_end_dates_are_utc` — UTC-aware datetimes
- `test_chunk_duration_strings_correct` — 1m→"30 D", 1h→"1 Y", 4h→"1 Y", 1D→"1 Y", 1M→"1 Y"
- `test_chunk_planner_supports_all_seven_timeframes`
- Pacing: sleep enforced, rate tracking, `is_rate_safe()` returns False at limit

### Definition of Done
- [ ] ChunkPlanner produces correct chunks for all 7 timeframes
- [ ] Chunks are reverse-chronological
- [ ] PacingCoordinator enforces configurable delay
- [ ] Rate tracking works correctly
- [ ] All chunk planner + pacing tests pass

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-009/`

### Dependencies
- MDRT-001, MDRT-004 (settings for `IB_PACING_DELAY_SEC`)

### Risk Level
- **Low**

---

## MDRT-010: IbHistoricalDataCollector

### Purpose
Implement the `HistoricalDataCollector` ABC and `IbHistoricalDataCollector` that fetches bars via `reqHistoricalData()` callbacks.

### Source Docs
- `03-adapter-interface.md` §3.2 (HistoricalDataCollector ABC, IB implementation)
- `07-exceptions.md` (EmptyResponseError, ProviderPacingError)
- `09-testing.md` (CallbackReplaySession, transcript fixture format)

### Exact Requirements from Docs
- ABC with `fetch_chunk(instrument, spec, chunk_end, duration_str)` → `list[dict]`
- `IbHistoricalDataCollector`:
  - Calls `reqHistoricalData()` with: `endDateTime`, `durationStr`, `barSizeSetting` from `BAR_SIZE_MAP`, `whatToShow`, `useRTH`, `formatDate=2`
  - Collects `historicalData` callbacks, buffers in memory until `historicalDataEnd`
  - BAR_SIZE_MAP: `{"1m": "1 min", "5m": "5 mins", "15m": "15 mins", "1h": "1 hour", "4h": "4 hours", "1D": "1 day", "1M": "1 month"}`
  - Uses ChunkPlanner for multi-chunk fetches
  - Uses PacingCoordinator between chunks
  - Returns provider-native records (raw dicts, NOT normalized)

### In Scope
- `src/market_data/adapters/collector.py` — ABC + `IbHistoricalDataCollector`
- Transcript fixture file for SPY 1m bars
- Unit tests via `CallbackReplaySession`

### Out of Scope
- Normalizing the returned records (MDRT-012)
- Persisting raw transcripts (MDRT-011)

### Expected Files
```
src/market_data/adapters/collector.py
tests/adapters/transcripts/spy_1m_2024_01_03.jsonl
tests/adapters/test_ib_collector.py
```

### Required Tests
- Replay session → `fetch_chunk` returns list of bar dicts
- Bar dict has expected IB fields: `date`, `open`, `high`, `low`, `close`, `volume`, `barCount`, `WAP`
- Empty response → `EmptyResponseError`
- Pacing delay applied between chunks
- BAR_SIZE_MAP contains all 7 timeframes

### Definition of Done
- [ ] `HistoricalDataCollector` ABC defined
- [ ] `IbHistoricalDataCollector` implements ABC
- [ ] Uses ChunkPlanner and PacingCoordinator
- [ ] Returns raw provider-native dicts
- [ ] BAR_SIZE_MAP has all 7 entries
- [ ] `tests/adapters/test_ib_collector.py` passes with replay session

### Audit Bundle
- Folder: `docs/requirements/MDRT/implementation-tasks-f/audit-bundles/mdrt-010/`

### Dependencies
- MDRT-007 (IbSession), MDRT-008 (IbContractResolver for Instrument), MDRT-009 (ChunkPlanner, PacingCoordinator)

### Risk Level
- **Medium** (ibapi callback buffering is the core IB integration challenge)
