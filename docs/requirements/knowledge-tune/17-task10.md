Yes — here is the **exact next instruction for the agent**.

---

# Agent task: fix remaining correctness failures before moving forward

You are working on an existing Python trading-lesson knowledge pipeline.

## Goal

Do a **narrow correctness pass** to fix the remaining failures in the latest rerun.

Do **not** redesign the pipeline.
Do **not** broaden scope.
Do **not** change extraction prompting.
Do **not** relax ML safety.

The pipeline is close, but it is **not accepted yet** because of five remaining issues:

1. one evidence row is still attached as a `positive_example` to **contradictory rules**
2. ML outputs are active before compatibility is fully proven
3. Russian `*_ru` fields exist but are still not populated
4. `concept_graph.json` still contains many relations with empty `source_rule_ids`
5. `rag_ready.md` still does not emit timestamps

Your job is to fix exactly those issues.

---

# Scope rules

## Do

* keep the current JSON-first architecture
* keep backward compatibility
* keep the Russian-first source layer
* keep canonical IDs
* make fixes deterministic and testable
* prefer conservative behavior over aggressive labeling

## Do not

* do not redesign evidence extraction
* do not add new LLM steps
* do not broaden ML eligibility
* do not remove legacy fields that exporters/tests still use
* do not change unrelated schema

---

# Part 1 — fix contradictory positive-example assignment

## Problem

A single evidence row can still become a `positive_example` for two **opposite** rules.

That must never happen.

## Required behavior

An evidence row may be attached as a `positive_example` only if it is semantically compatible with the target rule.

If the same evidence row is linked to multiple incompatible rules, then:

* keep the evidence row in `evidence_index.json`
* but do **not** allow it to remain ML-eligible for contradictory rules
* safe fallback:

  * keep only the best compatible rule, or
  * downgrade the evidence to `illustration` if compatibility cannot be resolved conservatively

## Implementation

Create a helper like:

```python
def infer_rule_direction(rule: dict) -> str:
    """
    Returns one of:
    - bullish_above
    - bearish_below
    - breakout_up
    - breakout_down
    - reversal_up
    - reversal_down
    - neutral
    - unknown
    """
```

Use deterministic signals from:

* `concept_id`
* `subconcept_id`
* `rule_text`
* `rule_text_ru`
* conditions / invalidations
* source events if needed

Then create:

```python
def is_positive_example_compatible(
    rule: dict,
    evidence: dict,
    source_events_by_id: dict[str, dict],
) -> bool:
    ...
```

### Minimum compatibility rules

Block positive-example attachment when any of the following is true:

* target rules have opposite directional meaning
* evidence source events do not support the rule’s direction
* the same evidence would simultaneously confirm bullish and bearish outcomes
* compatibility is ambiguous and cannot be resolved safely

### Required pipeline changes

Apply this compatibility gate in all three places:

1. rule-side example ref attachment
2. `ml_manifest.examples` creation
3. `labeling_manifest.tasks` creation

### Safety rule

If compatibility cannot be confidently proven:

* do **not** emit ML example
* do **not** emit labeling task

Conservative empty output is preferred over wrong output.

---

# Part 2 — temporarily harden ML output gating

## Problem

ML outputs are now active even though contradictory assignment is still unresolved.

## Required behavior

Until compatibility is correctly enforced, ML outputs must stay conservative.

## Implementation

Update the final ML eligibility logic so evidence is excluded from ML outputs if:

* it is attached to more than one rule with conflicting direction
* compatibility check returns `False`
* compatibility check returns `unknown`
* rule polarity cannot be safely resolved
* evidence semantics are weak / generic / ambiguous

Add a helper like:

```python
def is_evidence_safe_for_ml(
    evidence: dict,
    linked_rules: list[dict],
    source_events_by_id: dict[str, dict],
) -> bool:
    ...
```

Use this in:

* `build_ml_examples(...)`
* `attach_rule_example_refs(...)`
* `build_labeling_tasks(...)`

### Temporary expected result

It is acceptable if after this patch:

* `ml_manifest.examples == []`
* `labeling_manifest.tasks == []`

That is better than incorrect positive examples.

---

# Part 3 — populate Russian multilingual fields

## Problem

The new multilingual fields exist but are still empty.

Examples currently needing backfill:

* `normalized_text_ru`
* `rule_text_ru`
* `concept_label_ru`
* `subconcept_label_ru`
* `summary_ru`

## Required behavior

For Russian-source lessons, populate the `*_ru` fields from existing legacy payload fields.

Do **not** force translation.
Leave `*_en` fields empty unless real English content already exists.

## Implementation

### KnowledgeEvent backfill

If:

* `source_language == "ru"`
* `normalized_text_ru` is empty
* `normalized_text` is non-empty

then:

```python
normalized_text_ru = normalized_text
```

If:

* `concept_label_ru` empty
* `concept` exists

then:

```python
concept_label_ru = concept
```

If:

* `subconcept_label_ru` empty
* `subconcept` exists

then:

```python
subconcept_label_ru = subconcept
```

### RuleCard backfill

If:

* `source_language == "ru"`
* `rule_text_ru` empty
* `rule_text` exists

then:

```python
rule_text_ru = rule_text
```

Similarly backfill:

* `concept_label_ru`
* `subconcept_label_ru`

### EvidenceRef backfill

If:

* `source_language == "ru"`
* `summary_ru` empty
* `summary` exists

then:

```python
summary_ru = summary
```

### Important

Do not remove or rename these legacy fields:

* `normalized_text`
* `rule_text`
* `concept`
* `subconcept`
* `summary`

The new fields are additive.

---

# Part 4 — tighten concept graph provenance

## Problem

`concept_graph.json` still contains many relations with empty `source_rule_ids`.

Those relations are not trustworthy enough.

## Required behavior

No graph relation should be emitted unless it is backed by at least one supporting rule ID.

## Implementation

### Hard rule

Before writing any relation:

```python
if not source_rule_ids:
    skip_relation()
```

### Keep only these relation types

Allowed relation types:

* `has_subconcept`
* `has_condition`
* `has_invalidation`
* `has_exception`
* `co_occurs_with`

Optional:

* `related_to` only if it has explicit rule support and is not redundant

### Remove mirrored structural duplication

Do not emit both directions for the same structural relation.

Example:

* keep `has_subconcept`
* remove reverse duplicate variants like `parent_of` / `child_of`

### Co-occurrence threshold

Emit `co_occurs_with` only if support count is at least `2`.

### Relation schema

Every relation must have:

```python
{
  "relation_id": "...",
  "source_id": "...",
  "target_id": "...",
  "relation_type": "...",
  "weight": 1,
  "source_rule_ids": [...],
  "metadata": {...}
}
```

### Acceptance condition

After rerun:

* every relation has non-empty `source_rule_ids`
* relation count is reduced
* graph remains non-empty
* graph becomes more interpretable

---

# Part 5 — restore timestamps in `rag_ready.md`

## Problem

`rag_ready.md` still has no `[MM:SS]` timestamps.

## Required behavior

Emit timestamps when they can be derived from structured rule provenance.

Do not fabricate timestamps.

## Implementation

Create:

```python
def resolve_rule_timestamp(rule: dict, events_by_id: dict[str, dict]) -> str | None:
    ...
```

Resolution order:

1. collect source events via `source_event_ids`
2. look for usable timestamp fields on those events
3. prefer earliest confident timestamp
4. format as `MM:SS`

Rendering rule:

* if timestamp resolved:

  * render `[MM:SS] Rule: ...`
* if not:

  * render rule without timestamp

### Important

Do not collapse missing values to `[00:00]`.

---

# Tests to add

## Test 1 — contradictory positive example is blocked

Create a fixture with:

* one bullish rule
* one bearish rule
* one evidence row linked to both

Assert:

* the same evidence cannot remain a `positive_example` for both rules
* ML example output stays empty or only keeps one compatible rule

## Test 2 — ML safety stays conservative under ambiguity

If rule/evidence compatibility is unresolved:

* no ML example is emitted
* no labeling task is emitted

## Test 3 — Russian `*_ru` backfill works

For Russian-source event/rule/evidence objects, assert:

* `normalized_text_ru` populated from `normalized_text`
* `rule_text_ru` populated from `rule_text`
* `concept_label_ru` populated from `concept`
* `summary_ru` populated from `summary`

## Test 4 — graph relations require provenance

Assert:

* no relation is emitted with empty `source_rule_ids`

## Test 5 — no mirrored structural duplicates

Assert:

* if `A has_subconcept B` exists, reverse structural duplicate is absent

## Test 6 — RAG timestamps render when available

Given timestamped source events for a rule, assert:

* `rag_ready.md` includes `[MM:SS]`

## Test 7 — regression lessons stay clean

Both validation lessons must still preserve:

* no placeholder rule text
* no empty final `source_event_ids`
* no broken evidence backlinks
* no invalid wide-span `"line"` timestamp confidence

---

# Required acceptance criteria

The task is complete only if all of the following are true.

## Lesson 2

* no evidence row is a positive example for contradictory bullish/bearish rules
* if compatibility is unclear, ML outputs are empty
* `*_ru` fields are populated
* graph relations all have provenance
* `rag_ready.md` includes timestamps where available

## Sviatoslav lesson

* same conservative ML behavior
* `*_ru` fields are populated
* graph relations all have provenance
* `rag_ready.md` includes timestamps where available

## General

* backward-compatible legacy fields still exist
* no structural regressions in the core pipeline

---

# Deliverables

The agent must return:

1. updated code
2. updated tests
3. rerun outputs for both validation lessons
4. a short markdown note explaining:

   * how contradictory evidence assignment was fixed
   * how ML output gating was hardened
   * how `*_ru` fields are backfilled
   * how graph provenance filtering works
   * how RAG timestamps are resolved

---

# Final instruction

Implement this as a **strict correctness hardening pass**.

Do not move to new features.
Do not broaden ML generation.
Do not trust ambiguous evidence.
Make the outputs conservative, provenance-backed, and internally consistent first.
