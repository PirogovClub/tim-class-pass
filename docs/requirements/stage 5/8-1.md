Yes — here is the **clean copy-paste version** of the Step 8 junior-agent prompt, with no extra commentary:

```text
You are implementing the next roadmap step after ML Step 7.

IMPORTANT:
This is roadmap Step 8:
RUN BACKTESTS / WALK-FORWARD EVALUATION.

Read this entire prompt before touching code.

================================
MISSION
================================

Implement a real, time-aware, point-in-time-safe evaluation harness for the Step 7 dataset and baseline models.

This step is about:
- walk-forward fold generation
- forward-looking retraining/evaluation
- per-example prediction replay
- symbolic-vs-statistical baseline comparison
- policy-level evaluation
- calibration drift checks
- reproducible reports/manifests
- tests/docs/audit packaging

This step is NOT about:
- feature store redesign
- new label ontology
- new deterministic label system
- new deep model families
- live execution
- production serving
- full strategy engine productization

The architecture must remain:

1. Knowledge / RAG layer
   - rules
   - evidence
   - concept graph
   - provenance / explanation
   - similar-case retrieval

2. Market-data ML layer
   - Step 6 deterministic labels
   - Step 7 feature store
   - Step 7 baseline models
   - Step 8 walk-forward evaluation

3. Combined decision layer later
   - symbolic eligibility filters
   - model probabilities
   - similar-case retrieval
   - human-readable explanation

Do not blur these layers.

================================
PRIMARY OBJECTIVE
================================

Take the Step 7 dataset and baseline models and answer:

- do they generalize forward in time?
- do they stay calibrated out of sample?
- does the statistical baseline beat the symbolic baseline in forward slices?
- which classes/regimes/sessions/folds degrade?
- what threshold or policy combinations look promising?
- is the current modeling path strong enough to justify later Step 9 complexity?

At the end of this step, the next engineer should be able to:
- rerun walk-forward evaluation deterministically
- inspect per-fold train/validation/test windows
- inspect per-example predictions
- inspect fold-level and aggregate metrics
- compare symbolic vs statistical baselines
- inspect policy-threshold results
- judge whether Step 9 is justified

================================
DEFINITION OF DONE — READ FIRST
================================

You are NOT done when one test split score looks okay.
You are NOT done when one notebook runs.
You are NOT done when one static train/test evaluation exists.

You are done ONLY when ALL of the following are true:

1. Step 7 dataset artifacts are consumed as the evaluation source.
2. A machine-readable walk-forward config exists.
3. A deterministic fold builder exists.
4. Evaluation is explicitly time-aware / forward-looking.
5. Symbolic baseline is included in the comparison.
6. At least one statistical baseline is included in the comparison.
7. Per-example prediction replay artifacts are written.
8. Fold-level metrics are written.
9. Aggregate metrics are written.
10. Calibration checks across folds exist.
11. Policy-level evaluation exists.
12. Tests exist and pass.
13. Docs are updated.
14. Audit handoff docs are prepared.
15. Final audit zip is created and verified.

If any one of these is missing, the task is NOT COMPLETE.

At the end, repeat this Definition of Done and mark each item PASS or FAIL.

================================
STRICT SCOPE
================================

IN SCOPE:
- walkforward_config
- fold builder
- replay runner
- symbolic baseline evaluation
- statistical baseline evaluation
- calibration drift / forward calibration checks
- threshold / policy evaluation
- comparison reports
- manifests
- tests
- docs
- audit packaging

OUT OF SCOPE:
- no new feature families
- no new label generation system
- no sequence models
- no image models
- no live trading engine
- no broker integration
- no order simulator beyond simple policy evaluation
- no production monitoring system
- no broad refactor unrelated to evaluation

Keep this step narrow and operational.

================================
INPUTS YOU MUST USE
================================

Treat these as the source of truth:

1. Step 5 task definition artifacts
2. Step 6 deterministic label artifacts
3. Step 7 modeling dataset artifacts
4. Step 7 baseline model/training conventions
5. any existing manifest/integrity helpers already in the repo

At minimum you must consume or align with equivalents of:
- generated_labels.jsonl
- modeling_dataset.jsonl or modeling_dataset.parquet
- dataset_manifest.json
- split manifests or split-builder logic from Step 7
- symbolic baseline code
- logistic regression baseline code
- tree baseline code if present
- calibration code/reports from Step 7

Do not create a disconnected second evaluation universe.

================================
CORE DESIGN REQUIREMENT
================================

This step MUST be forward-looking.

Do NOT do a naive random split evaluation and call it complete.

Walk-forward means the system repeatedly:
1. trains on past data
2. optionally validates on later past/near-future data
3. tests on the next unseen forward slice
4. advances the window
5. repeats

This is the core of the step.

================================
WHAT THIS STEP MUST BUILD
================================

You must build these conceptual layers:

1. WALK-FORWARD CONFIG
   - defines folds, windows, policies, metrics, thresholds

2. FOLD BUILDER
   - creates deterministic time-aware train/val/test folds

3. REPLAY / EVALUATION RUNNER
   - trains or reloads baseline models per fold
   - predicts on the forward slice
   - writes per-example predictions

4. CALIBRATION EVALUATOR
   - evaluates calibration forward in time
   - checks probability reliability across folds

5. POLICY EVALUATOR
   - evaluates threshold/policy rules over predictions

6. REPORT / MANIFEST WRITER
   - writes fold metrics
   - aggregate metrics
   - model comparisons
   - policy reports
   - manifests

================================
REQUIRED DELIVERABLES
================================

Create the equivalent of the following deliverables.
If the repo already has better naming/location patterns, follow them exactly.

1. src/ml/walkforward_config.yaml
2. src/ml/backtest/fold_builder.py
3. src/ml/backtest/walkforward_runner.py
4. src/ml/backtest/prediction_replay.py
5. src/ml/backtest/policy_evaluator.py
6. src/ml/backtest/calibration_drift.py
7. src/ml/backtest/report_builder.py
8. tests/ml/test_walkforward_config.py
9. tests/ml/test_fold_builder.py
10. tests/ml/test_walkforward_runner.py
11. tests/pipeline/test_policy_evaluator.py
12. tests/ml/test_backtest_reports.py
13. docs/ml_step8_walkforward_backtests.md
14. RUN_ML_STEP8_AUDIT.md
15. ML_STEP8_HANDOFF.md

If the repo already has manifest/report helpers, integrate with them.

================================
REQUIRED OUTPUT ARTIFACTS
================================

This step must generate concrete output files, not just code.

At minimum produce the equivalent of:

1. walkforward_folds.json
2. backtest_predictions.jsonl
3. fold_metrics.json
4. walkforward_report.json
5. model_comparison_report.json
6. calibration_drift_report.json
7. policy_report.json
8. step8_manifest.json

Optional but encouraged:
9. threshold_sweep_report.json
10. per_class_fold_metrics.json
11. confusion_matrices.json

================================
REQUIRED WALK-FORWARD CONFIG
================================

Create a machine-readable config as source of truth.

At minimum include:
- task_id
- dataset_path or dataset source reference
- inclusion policy
- train window duration or sizing rule
- validation window duration or sizing rule
- test window duration or sizing rule
- step size
- minimum rows per fold
- grouping constraints if applicable
- models to evaluate
- calibration policy
- policy thresholds
- metrics to compute
- output paths or output root
- seed / reproducibility values

The config must be explicit.
Do not bury evaluation assumptions in ad hoc code.

================================
REQUIRED DATASET / EVAL POLICY
================================

You must define and implement a clear evaluation inclusion policy.

At minimum define:
- whether only gold+silver rows are used
- whether weak rows are excluded or evaluated separately
- whether ambiguous/excluded/skipped rows are always excluded
- how missing labels are handled
- how small classes/folds are handled
- whether some folds are skipped if insufficient support exists

Suggested first-pass policy:
- include gold + silver by default
- exclude ambiguous/excluded/skipped from primary walk-forward eval
- keep weak rows for optional sensitivity reports, not primary score
- log all exclusions explicitly

You may adjust this slightly, but the policy must be explicit and justified.

================================
FOLD BUILDER REQUIREMENTS
================================

The fold builder must be deterministic and time-aware.

At minimum define:
- fold_id
- train_start / train_end
- val_start / val_end (if used)
- test_start / test_end
- row counts by split
- label counts by split
- any grouped-exclusion logic

Preferred direction:
- grouped or time-aware split
- no naive random row-wise split
- avoid contamination from near-duplicate windows if possible

If the current dataset is small, still build the fold system generically and document limitations.

================================
PREDICTION REPLAY REQUIREMENTS
================================

For each fold and each evaluated model, write per-example prediction records.

Each prediction record must include at minimum:
- replay_id
- fold_id
- model_name
- candidate_id
- dataset_row_id
- anchor_timestamp
- timeframe
- actual_label
- predicted_label
- predicted_probabilities
- calibrated_probabilities if available
- confidence_tier
- point_in_time_safe
- threshold policy references if relevant
- metadata / provenance refs if available

Do not only write aggregate scores.
Per-example replay records are mandatory.

================================
MODELS TO EVALUATE
================================

Minimum required comparisons:

1. symbolic baseline
2. logistic regression baseline

Optional / recommended if Step 7 already supports it cleanly:
3. XGBoost or LightGBM baseline

Do not add deep models here.

If the tree baseline is unavailable in the environment, document it clearly and still complete:
- symbolic baseline
- logistic regression
- calibration / report comparison

================================
CALIBRATION REQUIREMENTS
================================

You must evaluate calibration forward in time.

At minimum:
- compare raw vs calibrated probabilities if calibration exists
- compute bucketed reliability summary or equivalent
- detect major forward drift in confidence behavior
- produce a calibration drift report

Do not treat calibration as optional just because this is a baseline stage.

================================
POLICY EVALUATION REQUIREMENTS
================================

You must implement a lightweight policy evaluation layer.

This is NOT a full execution engine.
It IS a structured way to test how predictions might be used.

At minimum support policies like:
- take top predicted class only
- require probability >= threshold
- require symbolic eligibility + model threshold
- compare symbolic-only vs model-only vs combined policy

For each policy, report at minimum:
- eligible row count
- class mix
- correctness / hit rate
- confidence distribution
- abstain / filtered count

If useful, include per-class policy performance.

================================
BACKTEST METRICS REQUIREMENTS
================================

At minimum compute and write:

Per fold:
- row count
- accuracy
- macro F1
- weighted F1
- per-class precision/recall/F1
- confusion matrix
- calibration summary if available

Aggregate:
- fold averages
- fold variability / stability
- symbolic vs statistical comparison
- policy comparison
- performance by:
  - class
  - timeframe if present
  - confidence tier if present
  - session bucket if present

Do not rely on one single score.

================================
POINT-IN-TIME SAFETY — MANDATORY
================================

This remains critical.

You must explicitly preserve point-in-time safety in evaluation.

Rules:
- folds must respect time order
- no test data may influence training or calibration improperly
- preprocessing must be fit only on train data, then applied forward
- any threshold tuning must not peek at test results
- no future data leakage into model inputs or policies

The manifest/report must explicitly state PIT assumptions.

================================
REQUIRED REPORT CONTENT
================================

At minimum the final reporting layer must answer:

1. How many folds were evaluated?
2. Which folds were skipped and why?
3. Which model performed best overall?
4. Which model was most stable across folds?
5. How did symbolic baseline compare to statistical baseline?
6. Which classes remain weak?
7. Did calibration degrade materially in forward slices?
8. Which policy/threshold combinations looked most promising?
9. What major limitations remain before live or backtest execution work?

================================
REQUIRED TESTING
================================

You must add tests.
Manual inspection alone is not enough.

Minimum required test groups:

1. CONFIG TESTS
- walkforward config loads
- required fields exist
- invalid config fails cleanly

2. FOLD BUILDER TESTS
- folds are deterministic
- folds respect time ordering
- train/val/test windows do not overlap incorrectly
- small/invalid folds are handled cleanly
- grouped exclusion logic behaves as expected on fixtures

3. RUNNER TESTS
- symbolic baseline replay runs
- logistic regression replay runs
- per-example predictions are written
- fold metrics are written
- aggregate report is written

4. POLICY EVALUATOR TESTS
- threshold policy works
- abstain/filter counts are correct
- symbolic+model combined policy works

5. CALIBRATION TESTS
- calibration drift report is written
- raw vs calibrated path is handled correctly
- insufficient-data case is handled explicitly

6. REPORT CONSISTENCY TESTS
- fold metrics sum correctly
- model comparison report matches fold results
- manifest counts align with written artifacts

7. REGRESSION TESTS
- no random split accidentally used in place of walk-forward
- test slice is never earlier than train slice
- ambiguous/excluded rows are not silently included in primary eval
- per-example predictions always include actual_label and predicted_label

================================
REQUIRED COMMAND-LINE / SCRIPT ENTRY
================================

Expose practical run commands.

At minimum there must be documented commands for:
- config validation
- fold generation
- walk-forward run
- policy evaluation
- report generation
- full audit run

Do not force the auditor to call library internals manually from a REPL.

================================
DOCUMENTATION REQUIREMENTS
================================

Create or update:

1. docs/ml_step8_walkforward_backtests.md
   Must explain:
   - purpose
   - why walk-forward is required
   - inclusion policy
   - fold policy
   - evaluated models
   - calibration policy
   - policy evaluation
   - outputs
   - known limitations

2. RUN_ML_STEP8_AUDIT.md
   Must include exact commands to:
   - validate config
   - run tests
   - generate folds
   - run walk-forward replay
   - inspect reports
   - build final audit zip

3. ML_STEP8_HANDOFF.md
   Must summarize:
   - what was implemented
   - what inputs are expected
   - what outputs are produced
   - what Step 9 should decide next
   - what is deferred
   - what the auditor should inspect

================================
AUDIT ARTIFACT REQUIREMENTS
================================

Create a final audit zip.

Include at minimum:

1. source/
   - changed code
   - walkforward config
   - fold builder
   - replay runner
   - policy evaluator
   - report/calibration logic
   - tests
   - docs
   - sample/fixture input files

2. examples/
   - walkforward_config snapshot
   - walkforward_folds.json
   - backtest_predictions.jsonl
   - fold_metrics.json
   - walkforward_report.json
   - model_comparison_report.json
   - calibration_drift_report.json
   - policy_report.json
   - step8_manifest.json

3. test_output.txt
4. RUN_ML_STEP8_AUDIT.md
5. ML_STEP8_HANDOFF.md

If the project already has a standard audit bundle shape, follow it exactly.

================================
WHAT YOU MUST NOT DO
================================

Do NOT:
- build live trading execution here
- add broker APIs
- add deep sequence models
- add chart-image models
- tune on the test fold
- use random splits and call it walk-forward
- leak future information into training/calibration/policy tuning
- stop after coding
- skip tests
- skip docs
- skip audit zip

================================
RECOMMENDED IMPLEMENTATION ORDER
================================

Follow this order:

STEP 1
Inspect Step 7 dataset artifacts, baseline code, manifests, and any split logic already present.

STEP 2
Define walkforward_config.yaml first.

STEP 3
Implement config loader/validator.

STEP 4
Implement deterministic fold builder.

STEP 5
Implement replay runner for symbolic baseline.

STEP 6
Implement replay runner for logistic regression.

STEP 7
Implement optional tree baseline replay if practical.

STEP 8
Implement calibration drift checks.

STEP 9
Implement policy evaluator.

STEP 10
Implement aggregate report/manifest writing.

STEP 11
Add tests.

STEP 12
Write docs and handoff.

STEP 13
Run tests, run the full walk-forward harness, capture outputs, and build audit zip.

================================
MANUAL VERIFICATION CHECKLIST
================================

Before you stop, manually verify ALL of these:

1. Step 7 dataset is actually consumed, not replaced with a fake dataset.
2. Walk-forward config is the source of truth.
3. Folds are time-ordered and deterministic.
4. Symbolic baseline is evaluated on real forward folds.
5. Logistic regression baseline is evaluated on real forward folds.
6. Per-example prediction replay files are real and coherent.
7. Calibration report is real and coherent.
8. Policy report is real and coherent.
9. Aggregate report matches fold metrics.
10. Tests pass.
11. Audit zip exists and contains required files.

================================
ACCEPTANCE STANDARD
================================

This step will be considered successful if:
- walk-forward evaluation is real and time-aware
- symbolic baseline is included
- at least one statistical baseline is included
- calibration is evaluated forward in time
- policy evaluation exists
- reports/manifests are produced
- tests exist and pass
- the system is ready for Step 9 architecture decisions without major guessing

This step will be rejected if:
- it is mostly prose
- it uses static random splits
- it lacks per-example replay outputs
- it lacks symbolic-vs-statistical comparison
- it ignores calibration
- it lacks tests
- it lacks audit packaging

================================
REQUIRED FINAL RESPONSE FORMAT
================================

When you finish, do NOT just say “done”.

Return a completion report with these sections:

1. Summary of what was implemented
2. Files added/changed
3. Walk-forward config summary
4. Inclusion policy
5. Fold policy
6. Models evaluated
7. Exact commands run
8. Test results summary
9. Walk-forward / policy evaluation summary
10. Docs added/updated
11. Audit bundle zip path/name
12. Known limitations / deferred items
13. Definition of Done checklist with PASS/FAIL per item

If any Definition of Done item is FAIL, say clearly that the task is not complete.

================================
FINAL DEFINITION OF DONE — READ AGAIN
================================

You are NOT done until ALL are true:

- Step 7 dataset is consumed
- walkforward config exists
- fold builder exists
- evaluation is time-aware
- symbolic baseline is included
- at least one statistical baseline is included
- per-example replay exists
- fold metrics exist
- aggregate metrics exist
- calibration checks exist
- policy evaluation exists
- tests pass
- docs are updated
- handoff is prepared
- audit zip is created and verified

If you finish coding but do not run tests and do not create the audit zip, the task is NOT COMPLETE.
```
