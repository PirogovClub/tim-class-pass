Use this as the **exact copy-paste prompt** for the junior agent:

```text
You are implementing Stage 6.4 of the trading knowledge pipeline.

Read this entire prompt carefully before touching the code.

================================
STAGE 6.4 MISSION
================================

Build a read-only Rule Browser / Analyst Tooling expansion on top of the approved Stage 6.3 Hybrid RAG Database and Retrieval API.

This is not a new retrieval engine.
This is not an ML phase.
This is not a write workflow.

Your job is to turn the minimal Stage 6.3 explorer into a practical analyst-facing browser that uses real Stage 6.3 data and APIs.

================================
DEFINITION OF DONE — READ FIRST
================================

You are NOT done when the code compiles.
You are NOT done when the UI looks finished.
You are NOT done when you have only changed templates.

You are done ONLY when ALL of the following are true:

1. A real read-only analyst browser exists on top of Stage 6.3 retrieval data.
2. A user can:
   - search the corpus
   - apply filters/facets
   - open a rule detail page
   - open an evidence detail page
   - open a concept detail page
   - open a lesson detail page
   - open related items
   - compare 2 to 4 items side by side
   - see provenance and timestamps
   - inspect ranking/explain information where available
3. All pages use real Stage 6.3 data and routes, not mocks.
4. Existing Stage 6.3 routes still work, or are cleanly preserved with backward-compatible behavior.
5. Tests are added and pass.
6. Documentation is added/updated.
7. Screenshots are captured.
8. A complete audit bundle zip is created.
9. The audit bundle zip includes source, tests, docs, screenshots, examples, rerun instructions, and test output.
10. You verify the zip contents before stopping.

If any one of these is missing, the task is NOT COMPLETE.

At the end of your work, repeat this Definition of Done and verify each item explicitly.

================================
STRICT SCOPE
================================

IN SCOPE:
- read-only analyst browser
- search page improvements
- rule detail page
- evidence detail page
- concept detail page
- lesson detail page
- related items view
- side-by-side compare view
- visible provenance/timestamps
- use of existing explain/facets/item retrieval outputs where appropriate
- tests
- docs
- audit packaging

OUT OF SCOPE:
- no new extraction pipeline work
- no new adjudication workflow
- no ML training
- no dataset builder
- no backtesting
- no auth/user accounts
- no broad frontend rewrite
- no unrelated refactors
- no speculative future code
- no replacing Stage 6.3 retrieval logic unless fixing a concrete bug needed for this step

Keep the step narrow and operational.

================================
GLOBAL RULES
================================

1. Reuse the approved Stage 6.3 retrieval and explorer foundation.
2. Use real data only. No hardcoded demo data in the final result.
3. Preserve backward compatibility where practical.
4. Prefer simple implementation consistent with the existing repo stack.
5. Provenance is mandatory. Do not hide it.
6. Stable deep links are mandatory.
7. URL state should reflect search/filter state where reasonable.
8. Missing data must produce clean empty/not-found states, never silent failures.
9. Do not stop after coding. You must run tests, verify routes manually, take screenshots, and assemble the audit zip.

================================
REQUIRED USER WORKFLOWS
================================

You must support all of these end to end.

WORKFLOW A — Search to rule detail
- user enters a query
- sees ranked results
- clicks a rule
- sees:
  - rule title or normalized text
  - rule id
  - lesson id
  - evidence links
  - timestamps
  - related concepts
  - related rules
  - linked events if available
  - provenance fields

WORKFLOW B — Search to evidence detail
- user opens an evidence item
- sees:
  - evidence id
  - lesson id
  - timestamps / time ranges
  - evidence type/modality if available
  - linked rules
  - linked events
  - linked concepts if available
  - short context summary if available

WORKFLOW C — Concept exploration
- user opens a concept page
- sees:
  - concept id
  - canonical concept
  - aliases
  - connected rules
  - connected lessons
  - related concepts / relations
  - counts if available

WORKFLOW D — Lesson exploration
- user opens a lesson page
- sees grouped material from that lesson:
  - rules
  - events
  - evidence refs
  - concepts

WORKFLOW E — Compare items
- user selects 2 to 4 items
- opens compare page
- sees aligned side-by-side fields
- minimum compare support:
  - rule vs rule
  - event vs event
- if cross-type compare is easy and natural, support it
- if not, do not overreach

WORKFLOW F — Explain result ranking
- user can inspect why a result ranked well
- expose when available:
  - lexical contribution
  - vector contribution
  - graph/concept contribution
  - alias/expansion contribution if available

================================
REQUIRED PAGES / ROUTES
================================

Use the existing explorer namespace and conventions already present in the repo.
Do NOT invent an unrelated UI subtree if one already exists.

Implement or extend the equivalent of the following pages:

1. Search page
2. Rule detail page
3. Evidence detail page
4. Concept detail page
5. Lesson detail page
6. Compare page

Follow existing route conventions exactly if the repo already has them.

Required behavior per page:

SEARCH PAGE
- query input
- search submit
- visible facets/filters
- results list
- deep links to items
- score visible on results where available
- compare selection mechanism
- filter state reflected in URL where reasonable

RULE DETAIL PAGE
Must display:
- stable rule id
- title or normalized rule text
- lesson id
- evidence ids
- timestamps
- related concepts
- related rules
- related events if available
- provenance block
- back links to search and lesson

EVIDENCE DETAIL PAGE
Must display:
- stable evidence id
- lesson id
- timestamps
- supporting linked rules/events
- provenance metadata
- back links to lesson and linked rules if available

CONCEPT DETAIL PAGE
Must display:
- concept id
- canonical label
- aliases
- graph relations
- linked rules
- linked lessons
- linked events if available

LESSON DETAIL PAGE
Must display grouped content:
- rules
- events
- evidence
- concepts

COMPARE PAGE
Must display aligned columns or sections for selected items.
Item ids must be visible and copyable.

================================
BACKEND / API REQUIREMENTS
================================

Do not bypass the existing retrieval service architecture if the repo already has API/service layers.

Reuse existing Stage 6.3 APIs wherever possible:
- search
- explain
- item detail
- related items
- lesson explorer
- facets

Add backend helpers or endpoints only if required for the Stage 6.4 workflows.

Allowed additions if needed:
- concept detail endpoint/helper
- compare endpoint/helper
- browser aggregate serializer/service
- lesson aggregation helper if existing lesson route is too thin

IMPORTANT:
Do not redesign retrieval scoring logic in this step unless fixing a real bug required for this browser workflow.

================================
REQUIRED DATA SHOWN IN UI
================================

For RULE items:
- id
- title/summary or normalized text
- lesson id
- evidence ids
- timestamps
- main concepts
- score on search page

For EVIDENCE items:
- id
- lesson id
- timestamps
- linked rules/events
- descriptor if available

For CONCEPT items:
- id
- canonical label
- aliases
- relation count or linked item count if available

For LESSON pages:
- lesson id
- counts of rules/events/evidence/concepts if available
- grouped links

PROVENANCE RULE:
If provenance exists in backend data, do not drop it from the UI unless it is completely unusable noise. When in doubt, show it.

================================
UI BEHAVIOR REQUIREMENTS
================================

The UI must be simple but usable.

Must-have:
- clear search input
- visible filters
- clickable result titles
- breadcrumbs or back links
- empty state
- not-found state
- compare action
- copyable ids
- stable URLs

Nice-to-have only if very small:
- collapsible explain panel
- highlighted terms
- badges for type/concept/lesson

Do not let nice-to-have items delay the core work.

================================
RECOMMENDED INTERNAL STRUCTURE
================================

Use the repo’s existing architecture.
Do not force these names if the repo already has better naming, but keep the same structural idea.

Recommended pieces:
1. Browser service layer / aggregators
2. Presentation serializers
3. Compare normalizer
4. Filter/facet query parser
5. Template / page rendering layer
6. Tests

Avoid putting complex dict-wrangling directly inside templates.

================================
DETAILED FUNCTIONAL REQUIREMENTS
================================

A. SEARCH PAGE
Must support:
- free-text query
- item type filter if supported
- lesson filter if supported
- concept filter if supported
- result cap/page controls if supported
- visible scores
- explain action
- compare selection

Results must include:
- title/summary
- item type
- lesson id
- score
- provenance hints
- deep link to detail page

Do not make raw JSON the primary UX.

B. RULE DETAIL PAGE
Required sections:
1. identity
2. core rule text / summary
3. provenance
4. evidence
5. related concepts
6. related items
7. lesson link

C. EVIDENCE DETAIL PAGE
Required sections:
1. identity
2. lesson/timestamp context
3. linked rules
4. linked events
5. linked concepts if available
6. navigation links

D. CONCEPT DETAIL PAGE
Required sections:
1. canonical concept name
2. aliases
3. linked rules
4. linked lessons
5. linked concepts / graph relations

This page must be meaningful. Do not reduce it to only “name + aliases”.

E. LESSON DETAIL PAGE
Required:
- grouped browsing by:
  - rules
  - events
  - evidence
  - concepts
- do not dump a flat ungrouped list

F. COMPARE PAGE
Support 2 to 4 items.
For each item show:
- id
- item type
- title/summary
- lesson id
- timestamps
- concepts
- evidence links
- provenance

If a field is missing for one item, render blank or “—”, not broken markup.

================================
TESTING REQUIREMENTS
================================

You must add tests. Manual clicking alone is not enough.

Minimum required test groups:

1. ROUTE / ENDPOINT TESTS
- search page loads
- rule detail page loads for valid id
- evidence detail page loads for valid id
- concept detail page loads for valid id
- lesson detail page loads for valid id
- compare page loads for valid ids
- invalid ids produce clean not-found state

2. CONTENT TESTS
Assert rendered pages contain critical expected fields:
- rule id visible
- lesson id visible
- evidence ids visible
- concept aliases visible
- lesson grouped sections visible

3. FILTER / URL STATE TESTS
Assert query params are parsed and reflected correctly.

4. COMPARE TESTS
Assert compare page renders multiple items and keeps ids aligned.

5. REGRESSION TESTS
Add coverage so provenance is not accidentally dropped from display models.

Strong preference:
If the repo already uses FastAPI/Starlette/TestClient or similar HTML tests, use the existing pattern. Do not introduce heavy browser automation unless the repo already uses it.

================================
DOCUMENTATION REQUIREMENTS
================================

You must update docs.

Create or update:
1. docs/stage6_4_rule_browser.md
   Must explain:
   - purpose
   - pages/routes
   - workflows
   - limitations
   - test strategy

2. RUN_STAGE6_4_AUDIT.md
   Must include exact commands for:
   - tests
   - app launch
   - manual verification
   - capturing screenshots if applicable
   - collecting example outputs
   - building audit zip if needed

3. STAGE6_4_HANDOFF.md
   Must summarize:
   - what was added
   - route list
   - what is deferred
   - known limitations
   - what auditor should inspect

If the repo already has a specific docs location/pattern, follow it exactly.

================================
AUDIT ARTIFACT REQUIREMENTS
================================

You must produce a complete audit bundle zip.

Required zip contents:

1. source/
   - changed code
   - relevant templates/static files
   - tests
   - docs
   - dependency context files if needed

2. examples/
   - example search outputs
   - example rule detail outputs
   - example evidence detail outputs
   - example concept detail outputs
   - example lesson detail outputs
   - example compare outputs

3. screenshots/
   - search page
   - rule detail page
   - evidence detail page
   - concept detail page
   - lesson detail page
   - compare page

4. test_output.txt
5. RUN_STAGE6_4_AUDIT.md
6. STAGE6_4_HANDOFF.md
7. REPRODUCIBILITY.md if required by project packaging conventions
8. requirements/pyproject context if needed for audit reproducibility

SCREENSHOT RULE:
Screenshots must be real.
Do not include placeholder filenames without actual images.

================================
MANUAL VERIFICATION CHECKLIST
================================

Before packaging the zip, manually verify ALL of these:

1. Search page opens
2. Search returns real results
3. Clicking a rule works
4. Clicking an evidence item works
5. Clicking a concept works
6. Clicking a lesson works
7. Compare works with at least 2 valid ids
8. Invalid id returns clean not-found state
9. Provenance is visible on detail pages
10. URLs are stable/shareable
11. Existing Stage 6.3 explorer behavior still works or is cleanly preserved

================================
WHAT YOU MUST NOT DO
================================

Do NOT:
- stop after coding
- skip tests
- skip docs
- skip screenshots
- skip examples
- skip audit zip
- silently break old routes
- rewrite retrieval for no reason
- turn this into a frontend migration
- add future ML code
- remove provenance for aesthetic reasons

================================
RECOMMENDED IMPLEMENTATION ORDER
================================

Follow this order:

STEP 1
Inspect the existing Stage 6.3 browser/explorer foundation.
Understand:
- current routes
- current templates
- current service layer
- current explain/facets/item endpoints

STEP 2
Write down the target Stage 6.4 route/page contract before changing code.

STEP 3
Add service/presenter helpers for browser-ready normalized data.

STEP 4
Add only missing backend helpers/endpoints.

STEP 5
Build pages:
- search first
- detail pages next
- compare page after that

STEP 6
Add tests before final polish.

STEP 7
Run manual verification.

STEP 8
Capture screenshots and assemble examples.

STEP 9
Write docs.

STEP 10
Build and verify audit bundle zip.

================================
EXPECTED DELIVERABLES
================================

By the end, your change set must include:

- improved search/browser page
- rule detail page
- evidence detail page
- concept detail page
- lesson detail page
- compare page
- service/presenter code
- tests
- docs
- screenshots
- examples
- audit zip

================================
ACCEPTANCE STANDARD
================================

This step will be considered acceptable if:
- the browser is clearly more than a minimal demo
- an analyst can navigate efficiently
- provenance is visible and preserved
- compare is real and useful
- implementation is read-only and narrow
- tests and audit artifacts are complete

This step will be rejected if:
- it is mostly cosmetic
- pages are thin wrappers around raw JSON
- compare is missing
- concept browsing is weak
- provenance is hidden
- tests are missing
- audit zip is incomplete

================================
REQUIRED FINAL RESPONSE FORMAT
================================

When you finish, do NOT just say “done”.

Return a completion report with these sections:

1. Summary of what was implemented
2. Routes/pages added or changed
3. Backend/services added or changed
4. Tests added and exact test command run
5. Test results summary
6. Manual verification summary
7. Docs added/updated
8. Audit bundle zip path/name
9. Known limitations / deferred items
10. Definition of Done checklist with each item marked PASS/FAIL

If any Definition of Done item is FAIL, say clearly that the task is not complete.

================================
FINAL DEFINITION OF DONE — READ AGAIN
================================

You are NOT done until ALL are true:

- real read-only analyst browser exists
- search works
- filters/facets work
- rule detail page works
- evidence detail page works
- concept detail page works
- lesson detail page works
- compare page works
- provenance is visible
- tests pass
- docs are updated
- screenshots are included
- examples are included
- audit zip is created
- zip contents are verified

If you finish coding but do not run tests and do not create the audit zip, the task is NOT COMPLETE.
```

**Confidence: High — this is aligned to the approved 6.3 state and your audit-driven workflow style.**
