export function ConceptAliases({ aliases }: { aliases: string[] }) { return <section data-testid="concept-aliases" className="space-y-2"><h2 className="text-lg font-semibold text-slate-900">Aliases</h2>{aliases.length ? <ul className="list-disc space-y-1 pl-5 text-sm text-slate-700">{aliases.map((alias) => <li key={alias}>{alias}</li>)}</ul> : <p className="text-sm text-slate-500">No aliases</p>}</section>; }
