import { Link } from 'react-router-dom';
export function LessonTopConcepts({ concepts }: { concepts: string[] }) { return <section data-testid="top-concepts" className="space-y-2"><h2 className="text-lg font-semibold text-slate-900">Top Concepts</h2>{concepts.length ? <div className="flex flex-wrap gap-2">{concepts.map((concept) => <Link key={concept} to={`/concept/${encodeURIComponent(concept)}`} className="rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-xs font-medium text-slate-700 hover:border-teal-300 hover:text-teal-700">{concept}</Link>)}</div> : <p className="text-sm text-slate-500">No top concepts found</p>}</section>; }
