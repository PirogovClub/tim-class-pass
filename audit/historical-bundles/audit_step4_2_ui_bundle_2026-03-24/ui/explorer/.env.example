# Optional: direct API base for production builds or when not using the Vite dev proxy.
# Leave unset in development so requests use same-origin `/browser/*` (proxied to FastAPI).
# VITE_BROWSER_API_BASE=http://127.0.0.1:8000
